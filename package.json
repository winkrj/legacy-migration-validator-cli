{
  "name": "legacy-migration-validator-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only Markdown validator PoC for legacy migration documentation.",
  "type": "module",
  "bin": {
    "legacy-validator": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "tsx src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
